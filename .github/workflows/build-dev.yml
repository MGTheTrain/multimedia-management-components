name: Build Dev

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/build-reusable.yml
    with:
      upload_packages: true
      revision_number: beta-${{ github.run_number }}
